using System;

public class Prueba {
   static void Main()
   {
        var referencia;
        if (true)
            referencia = "dfa";
        else
            referencia = 3;
        //Console.WriteLine(referencia.Length);
   }
}
